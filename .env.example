# Supabase credentials — use the grainiq-dev project for development.
# Find these in Supabase: Project Settings -> API
# Copy this file to .env.local (gitignored) and fill in the values.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
