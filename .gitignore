node_modules/
dist/
.env
.env.local
.env.*.local
npm-debug.log*
.DS_Store
.vercel
.claude/settings.local.json
supabase/.temp/

# Project brain — sensitive business strategy, financials & named contacts.
# Kept on local disk, never committed/pushed.
CLAUDE.md

# Validation micrographs may be confidential (customer/dissertation samples).
# Keep image fixtures local; the manifest (numbers only) and harness are tracked.
validation/fixtures/*
!validation/fixtures/.gitkeep
